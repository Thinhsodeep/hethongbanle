# PHP / Composer
/vendor/
composer.phar
composer.lock

# Environment & secrets — không push cấu hình thật
.env
.env.*
!.env.example
config/database.local.php

# Cursor / agent (chỉ dùng local, không push)
.cursorrules
AGENT_PERSON1.md

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs & cache
*.log
/storage/
/cache/

# Uploaded assets (giữ cấu trúc thư mục nếu cần)
/public/images/uploads/*
